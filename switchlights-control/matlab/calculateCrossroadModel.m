function [ mfParams mfCounts fRules modelParams ] = calculateCrossroadModel(loadModel, trainData, type)
%CALCULATECROSSROADMODEL Summary of this function goes here
%   Detailed explanation goes here
  fprintf('loadModel: %s\n', loadModel);
  fprintf('trainData: %s\n', trainData);
  fprintf('type: %s\n', type);
  mfParams = 1;
  mfCounts = 2;
  fRules = 3;
  modelParams = 4;
end
